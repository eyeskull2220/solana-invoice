# REVIEWER — Builder PLAN-lock (batch 04)

**Seat:** REVIEWER (new batch)
**Stage:** Builder **PLAN** only
**Date:** 2026-08-27
**Artifact:** [PR #188](https://github.com/eyeskull2220/solana-invoice/pull/188) `docs/rgy-2026-08-27/builder/PLAN-lock.md` (head `5ac1d3e`)
**This file:** judgment only. **No implement.** No DESIGN page. No shop HTML, kits, CSS, mail, or Surge.

GREEN for this stage only if the scorecard has **no RED and no YELLOW**. One yellow fails GREEN.

---

## Verdict

**PLAN stage: GREEN.** There is **no RED** and **no YELLOW** on this file.

Old pack [PR #182](https://github.com/eyeskull2220/solana-invoice/pull/182) scored [PR #172](https://github.com/eyeskull2220/solana-invoice/pull/172) **YELLOW** because the sequence jumped plan reviewer → **CODE** and labeled chrome “enough for CODE, not a second DESIGN punch.” This batch scores the rewrite only. Old notes are **not** the score. Each gate was re-read on #188.

The shop job from #172 stays. **When** it runs is what this lock changes. Sequence on this page is: this PLAN lock → **a different PLAN reviewer** (this batch) → **DESIGN** → **a different design reviewer** → **CODE**. CODE is last.

**After this reviewer: DESIGN.** Not live Surge. Not CODE.

This batch does not score live [sovereignforge.surge.sh](https://sovereignforge.surge.sh/). Live USDC / `Disallow: /` is world-state, not a defect in this markdown. Leftover [PR #111](https://github.com/eyeskull2220/solana-invoice/pull/111) is **not the shop**. [PR #137](https://github.com/eyeskull2220/solana-invoice/pull/137) chalkboard shop HTML is **PARKED CODE**. It is not this PLAN going green. HTML was not scored.

The plan’s own §15 table is **not** the colour. This page does not grade itself GREEN by copying “yellow designed out.”

---

## What was scored (and what was not)

| Object | Used as |
| --- | --- |
| PR #188 `PLAN-lock.md` | **The only graded object.** One markdown file. |
| PR #182 `REVIEW-03-plan.md` | The YELLOW pack this rewrite answers. Bar, not re-scored as the plan. |
| PR #172 `PLAN-one-pass.md` | History. Shop-job GREENS this lock must keep. **Not** the page a later agent follows for sequence. |
| PR #161 / #122 / #125 | Punch-list pack. Not re-opened. |
| PR #159 `03-adv-design.md` | Proof DESIGN has not closed (chalkboard costume, coin in first viewport). **Later DESIGN seat.** Not this file. Not a design review in this batch. |
| PR #137 chalkboard shop HTML | **PARKED CODE.** Exhibit that CODE already exists in git and is not this PLAN. **HTML not scored.** |
| Live shop, leftover `main` HTML, kit hosts | **Out of scope.** This file scores the PLAN page, not CODE. |

A later Builder agent follows **#188** for sequence and next seat. Not #172’s three-step skip. Not #137 as a PLAN green. Not a live cutover as a PLAN green.

---

## Frozen six (still locked)

| Kit | Face price |
| --- | ---: |
| Menukaart + allergenen | **€199** |
| Sponsorblad vzw | **€199** |
| Vakman one-pager | **€249** |
| Inbox-ops | **€299** |
| Lid-inschrijving | **€349** |
| Club- of vzw-site | **€900** |

Integers. No seventh chip. A demo is a demo.

---

## This-run checks (2026-08-27)

| Check | Result |
| --- | --- |
| PR #188 files | **One** markdown file. No shop HTML. No kits. No CSS. No mail. No Surge. |
| Sequence step 3 | **THEN DESIGN** (not live Surge, not CODE). Step 5 is CODE. “Do not paraphrase CODE into step 3.” |
| Next seat after this reviewer | **DESIGN.** Repeated in §0, §1, §11, §16. Not Surge. Not CODE. |
| “enough for CODE, not a second DESIGN punch” as an instruction | **Killed.** Appears only as the sentence being retired (§0, §11, §15, §16). |
| “Then CODE. Not this run.” as next-seat | **Killed.** Replaced by DESIGN after this reviewer. |
| Chrome tokens picked (`--board` / cream / Inter) as CODE input | **No.** Named as things this PLAN does **not** pick. CODE does not copy chalkboard CSS from this PLAN. |
| PR #159 treated as closed DESIGN | **No.** Later design seat. RED (chalkboard costume, coin in first viewport) stays. Do not start DESIGN in this PLAN PR. |
| PR #137 treated as this PLAN going green | **No.** Listed as why it is not this job. This review does not score that HTML. |
| Face six in §5 | **€199 / €199 / €249 / €299 / €349 / €900.** Order menu → sponsor → vakman → inbox-ops → lid → club. |
| Public-face lock | No `USDC` / `Solana` / `Phantom` / `crypto` / wallet on the public face. Betalen H1 is **Betaalgegevens na akkoord.** |
| Privacy copy in §8 | STORE **Versie 3 — 27 augustus 2026**. Sasha · Geel · `sasha.de.vree.rene@gmail.com`. Host/mailhost **niet geverifieerd**. Gmail buiten de EER alleen om te antwoorden. |
| robots | `User-agent: *` then `Allow: /` on shop **and** six kit hosts. `Disallow: /` is a fail. |
| Origin | `shop/sovereignforge/` → `https://sovereignforge.surge.sh/` only. Preview then cutover. **Do not surge `main`.** |
| Leftover #111 | **Not the shop.** Do not merge. Do not port €9/€49. |
| This review wrote HTML / sent mail / ran Surge | **No.** |

Do **not** grep the PLAN markdown for coin tokens and call that a fail. This page names the gate. The prescribed public-face copy is what must pass. HTML was not grepped. Live origin was not grepped.

---

## Closed from #182 (not copied as this grade)

|#182 attack | Was | Why #188 closes it |
| --- | --- | --- |
| Sequence is this PLAN → plan reviewer → **CODE** | **YELLOW** | §1 is five steps. Step 3 is **DESIGN**. Step 4 is a **different design reviewer**. Step 5 is CODE. Copy this order. Do not paraphrase CODE into step 3. |
| “Look and chrome (enough for CODE, not a second DESIGN punch)” | **YELLOW** | **Killed.** §11: chrome is DESIGN. This PLAN does not pick `--board` / cream / Inter. CODE implements the DESIGN that survives design review. |
| Next seat: “Then CODE. Not this run.” | **YELLOW** | Next seat after the PLAN reviewer of **this** file is **DESIGN** (docs, not a ship, not live Surge). |
| Do not treat a later CODE GREEN, or a live cutover, as a PLAN GREEN | Bar 4 | §0, §12, §13. A PLAN GREEN on this file is not a CODE start. Live origin matching the job is Builder CODE, after design review. |

Shop-job GREENS from #172 were already closed in writing. This lock **keeps** them. It does not reopen the punch-list reds from #161.

---

## Scorecard

Worst row fails the stage. GREEN only if every row is GREEN.

| Gate | Score |
| --- | --- |
| One complete EUR-first shop pass vs punch list | **GREEN** |
| EUR-first face (no USDC / Solana / Phantom / crypto / wallet on the public face) | **GREEN** |
| Six kits only (table above) | **GREEN** |
| robots `User-agent: *` / `Allow: /` on shop + six kit hosts | **GREEN** |
| No junk SKUs; leftover #111 is not the shop | **GREEN** |
| Privacy STORE **Versie 3** | **GREEN** |
| Betalen = **Betaalgegevens na akkoord** | **GREEN** |
| Kill freelancer line, leftover SKUs, junk chips | **GREEN** |
| Origin: `shop/sovereignforge/` → `https://sovereignforge.surge.sh/` only; do not surge `main` | **GREEN** |
| Operator stage order (this PLAN lock → PLAN reviewer → **DESIGN** → design review → CODE) | **GREEN** |
| Chrome is DESIGN; killed “enough for CODE, not a second DESIGN punch”; CODE does not pick `--board` / cream / Inter from this PLAN | **GREEN** |
| #159 stays a later DESIGN seat (RED: chalkboard costume, coin in first viewport); not this file | **GREEN** |
| #137 parked CODE is not this PLAN going green; HTML not scored | **GREEN** |
| Adjacent: fake KBO / mail send / FACTUUR stamp / this review surged | **GREEN** |
| **PLAN stage** | **GREEN** |

**RED rows: none.**
**YELLOW rows: none.**
GREEN is therefore available.

---

## 1. One complete pass vs punch list — **GREEN**

#161 refused GREEN because the written plan was strip / Allow / hide-address / spawn the next FIX seat.

#188 is still **one** later CODE job, not that sequence. It names:

- Origin of record: git `shop/sovereignforge/` **is** what deploys to `https://sovereignforge.surge.sh/` (preview, then cutover) — **after** DESIGN and design review.
- Closed file set: `index.html`, `pakketten.html`, `betalen.html`, `contact.html`, `privacy.html`, `robots.txt`. `styles.css` / `fonts/` named by **DESIGN after design review**, not chalkboard-from-this-PLAN.
- Closed six, each with a live demo host before akkoord.
- Featured kit euro wraps in the **same** later CODE pass (inbox-ops drops USDC + freelancer line).
- Kill list: freelancer, live `SITE.md`, “elf live kits”, junk SKUs, #111.
- Secretary acceptance on the live origin after cutover.
- Explicit: leftover #111, root `index.html` / `catalog.html` / `solana-invoice.html`, and `treasury-tools.surge.sh` are **not** the shop. Do not surge from `.`.

That is still the one-pass page. It is not a punch list. The done-check `rg` block is acceptance for the **same** later CODE pass, not a FIX-seat spawn, and not a start-CODE licence for this reviewer.

---

## 2. EUR-first face — **GREEN**

Face prices in the file are **€199 / €199 / €249 / €299 / €349 / €900**. Integers.

The page forbids `USDC`, `Solana`, `Phantom`, `crypto`, and wallet in `<title>`, `<h1>`, lede, chips, CTAs, and meta on shop pages. Home lede is euro OFFERTE copy. Pakketten kills “Charge blijft USDC.” Betalen is not a wallet panel (gate 7).

This matches the #172 lock: **no coin on the public face**.

---

## 3. Six kits only — **GREEN**

The closed-six table is the frozen six, in the locked order, with the locked euro integers. Uitkomst lines are six, not seven.

Drop-list is explicit: pipeline, Peppol chase, dual-invoice, Peppol Ready, één klus / 9 USDC invoice, CSV / form-to-email / RSS, #111 €9/€49 toys, km-log / freelance-contract / retainer / dagtarief / FAQ / UTM / waitlist / paywall / intake / link-in-bio as shop rows. Seizoenskaart and named-club stay **VOORBEELD demos**, not SKU #7.

ideas-builder #81 services stay off the shop table.

---

## 4. robots `Allow: /` — **GREEN**

The file writes, for the shop **and** each of the six kit hosts:

```
User-agent: *
Allow: /
```

`Disallow: /` is named a fail. Missing `robots.txt` is named as not Allow. CODE is told to land `shop/sovereignforge/robots.txt` and a `robots.txt` in each `kits/<slug>/` folder, then push Allow to the six existing treasury hosts in the **same later** pass — after DESIGN and design review.

Allow on leftover #111 is **not** treated as the shop. The origin in this plan is `sovereignforge.surge.sh`.

---

## 5. No junk SKUs; #111 is not the shop — **GREEN**

Explicit forbid: do not merge #111, do not port €9/€49 cards, do not treat hide-the-coin as the EUR pass. Root leftover invoice HTML is not the deploy folder. `catalog.html` is not on the shop origin.

Junk hosts are grepped out of shop HTML in the later CODE done-check. That is the #161 gate, still closed in writing.

---

## 6. Privacy STORE Versie 3 — **GREEN**

The file ships STORE **Versie 3 — 27 augustus 2026** as the shop `privacy.html` **copy lock**. Locks present:

- Wie: Sasha, natuurlijke persoon in Geel, `sasha.de.vree.rene@gmail.com`
- Host/mailhost logs **niet geverifieerd** → no cookie banner
- Gmail named as a dienst **buiten de EER**, alleen om te antwoorden; no SCC / adequacy claim
- No USDC / IBAN / kaartnummer on the page
- No AVG-conform badge
- One page on the shop origin; footer link required
- Do not restore Versie 1 “Betaling is on-chain in USDC”
- This PLAN PR does not write `privacy.html` onto disk as shop HTML

This is the GREEN fill-in from [PR #152](https://github.com/eyeskull2220/solana-invoice/pull/152), not a blank grep card. Live Versie 1 is world-state, not this score.

---

## 7. Betalen = Betaalgegevens na akkoord — **GREEN**

`betalen.html` H1 is **`Betaalgegevens na akkoord.`** Lede: no rekeningnummer, no ontvangstadres, no kaartformulier. Volgorde: kies kit → open voorbeeld → akkoord per mail → gegevens **apart, later**. Forbidden: treasury address `96BT6…buHk3`, mint, QR, copy-address, `USDC`, `Solana`, `Phantom`, radios that reveal a pay panel.

The page exists as a step, not a coin door. Charge copy is not on the face.

---

## 8. Kill freelancer / leftover SKUs / junk chips — **GREEN**

Shop copy must not say hire-me / freelancer / day-rate. Inbox-ops kicker is not “Demo freelancer · Antwerpen.” Root leftover “For freelancers…” is not deployed. `SITE.md` is not added to the git shop; live stub must 404 on cutover. No “Elf live kits” / “Alle 11 pakketten.”

Operator is not the freelancer. Team delivers.

---

## 9. Origin of record; do not surge `main` — **GREEN**

Live shop after cutover: **`https://sovereignforge.surge.sh/` only.** Git source: `shop/sovereignforge/`. CODE publishes that folder, never repo root. Do not surge `main`. Do not treat leftover #111 as the EUR pass.

This **PLAN** PR does not run Surge. §12 `npx surge` lines are illustrative for a **later** CODE seat, after DESIGN and design review. They are not a start-CODE licence for this reviewer.

---

## 10. Operator stage order — **GREEN**

This file is **PLAN**. Operator order on the page:

1. This PLAN lock — merge this markdown only.
2. **A different PLAN reviewer** scores this file (this batch).
3. **THEN DESIGN** (not live Surge, not CODE). Docs, not a ship. [PR #159](https://github.com/eyeskull2220/solana-invoice/pull/159) DESIGN 03 was **RED** (chalkboard costume, coin in first viewport) — later design seat, **not this file**.
4. **THEN a different design reviewer** scores that DESIGN page. GREEN DESIGN is required before CODE. A PLAN GREEN does not substitute.
5. **THEN CODE:** one pass onto `https://sovereignforge.surge.sh/` from `shop/sovereignforge/`. Preview, then cutover. Do not surge `main`. Leftover #111 is not the shop.

#172 wrote a three-step sequence that skipped 3 and 4. That skip is gone. The heading “this file does not skip DESIGN” is true on this page.

**After this reviewer: DESIGN. Not live Surge. Not CODE.**

---

## 11. Chrome is DESIGN — **GREEN**

**Killed:** the sentence “enough for CODE, not a second DESIGN punch.”

This PLAN does not pick `--board` / `--chalk` / `--brass` / `--wood`. It does not pick cream `#f4f1ea` / `#f6f3ee`. It does not pick Inter. It does not tell CODE to reuse live chalkboard tokens, Young Serif, Atkinson, or Iowan in the CODE pass.

[PR #159](https://github.com/eyeskull2220/solana-invoice/pull/159) `03-adv-design.md` is an adversarial design pass on the live chalkboard. **Overall RED.** Named reds include **chalkboard costume** and **coin in the first viewport**. That file is a later DESIGN seat. **Not this file. Not this PR.** Do not treat #159 as closed DESIGN. Do not collapse DESIGN into CODE.

IA that **is** PLAN (secretary job): home is one hero + six cards; pakketten is stacked cards, not a 936px table; at 390px, no horizontal scroll. Paint, type, and costume wait for DESIGN.

CODE implements the DESIGN that survives design review. CODE does not invent chrome from this PLAN.

§6 “shared chrome” is copy and IA (nav, footer, stamp). Paint is DESIGN. That split holds.

---

## 12. Parked CODE is not this PLAN — **GREEN**

[PR #137](https://github.com/eyeskull2220/solana-invoice/pull/137) is chalkboard shop HTML in git. It is **PARKED CODE**. This review does **not** score that HTML. This PLAN going GREEN does not green #137, does not merge it, and does not skip DESIGN.

#188 lists #137 as why it is not this job (partial git shop; kit hosts still USDC; not cut over; leftover root mixed in). That is the correct parking. Scoring this markdown GREEN is not leftover-#137 shop progress.

---

## Adjacent GREEN (does not save a skipped sequence — none to save)

- No invented KBO / `BE0`. Stamp remains `KBO/BTW: nog niet toegekend`.
- This PLAN does not send mail. This review did not send mail.
- Stamp is OFFERTE / VOORBEELD, not FACTUUR. Negation `geen wettelijke factuur` is allowed.
- This review did not write shop HTML, did not publish to Surge, did not surge `main`, did not start DESIGN, did not start CODE.

---

## Notes (not extra colours)

1. **GREEN on PLAN is not a CODE start.** §12 still contains preview/cutover `npx surge` commands. They run only after DESIGN is written **and** a **different** design reviewer greens **that** DESIGN. This reviewer does not run them.

2. **Live origin is still world-state.** USDC on home, `Disallow: /`, privacy Versie 1 — not this score. Do not GREEN this file because the shop moved. Do not hold this file until the shop moves.

3. **#172 is not rewritten.** Teammates read **this lock** (#188) for sequence and next seat. Do not dispatch CODE from #172.

4. **CODE done-check omits `wallet` from the `rg` pattern** while §2 and §13 still forbid wallet on the public face. The lock is the gate. A later CODE seat should grep `wallet` too. That gap is not a yellow on this PLAN: the instruction is already “no wallet on the public face.”

5. **§8 HTML fence is copy lock**, not shop HTML on disk. PR #188 did not add `privacy.html`. This review did not either.

---

## Bar for GREEN (this artifact = this Builder **plan**)

|#182 bar | After #188 |
| --- | --- |
| 1. Keep the one-pass shop job, frozen six, Versie 3, betalen lock, robots Allow, #111 forbid, do-not-surge-`main`. | **Pass.** §2–§10, §12–§13. |
| 2. Rewrite Sequence so it cannot be read as plan-reviewer → CODE. Insert DESIGN, then design review, before CODE. | **Pass.** §1. |
| 3. Kill “enough for CODE, not a second DESIGN punch” and “Then CODE. Not this run.” Next seat after this reviewer is DESIGN, not live Surge. | **Pass.** §0, §1, §11. |
| 4. Do not treat a later CODE GREEN, or a live cutover, as a PLAN GREEN. | **Pass.** §0, §12. |

Until a later **DESIGN** page (not #159 as-closed, not this file) and a **different** design reviewer, CODE does not start.

---

## This run

| Did | Did not |
| --- | --- |
| Read PR #188 `PLAN-lock.md` as the only graded object | Design review of chalkboard vs paper |
| Re-scored from zero against the #182 yellow bar and the #172 GREENS | Copy #182’s GREEN rows as automatic |
| Locked origin, six kits, Versie 3, betalen, robots, kill list as GREEN in writing | CODE review, shop HTML, kits, CSS |
| Confirmed sequence is PLAN reviewer → **DESIGN** → design review → CODE | Treat #137 HTML as this PLAN going green |
| Confirmed the killed punch sentence is gone as an instruction | Pick `--board` / cream / Inter; treat #159 as closed DESIGN |
| Scored this PLAN **GREEN** (no red, no yellow) | Mail, Surge publish, surge `main`, merge or “fix” #111 |

**PLAN stage: GREEN.** Next seat after this reviewer: **DESIGN.** Not live Surge. Not CODE.

---

## This file is not

- Not DESIGN. Not a design review of #159.
- Not CODE. Not a live-page PASS. Not leftover-#137 shop progress.
- Not permission to land `shop/sovereignforge/` or to `npx surge`.
- Not HTML. Not mail. Not Surge. Not a merge of #111.
- Not a rewrite of PR #172. Follow #188.

End. Docs only. No shop HTML. No mail. No Surge. Do not start CODE. Next seat: DESIGN.
