# CEO PLAN — adversarial, then RGY

**Seat:** CEO (one of six). Not a seventh seat.  
**Date:** 2026-08-27  
**File:** `docs/rgy-2026-08-27/ceo/02-adv-plan.md`  
**This run does not mail. It does not edit kit HTML. It does not treat a PR in this leftover repo as the shop moving.**

Adversarial first. RGY second. Design-out last: **NOW / NEXT / WAIT per lane, whole jobs.**  
If a plan is **one-kit unlocks** or **one-mail-then-hold**, it scores **RED**. Punch-list boards are scored against **one machine**.

---

## 0. What is being scored

Two pages already claim to be the CEO plan:

| Page | PR | Claim |
| --- | --- | --- |
| `docs/ultra-seats/CEO.md` | [#113](https://github.com/eyeskull2220/solana-invoice/pull/113) | Inventory + 15 misses + STARTABLE/BLOCKED/WAIT + **28-cell** 7-day Sell/Paper/Rails/Capabilities grid |
| `docs/rgy-2026-08-27/ceo/FIX-BOARD.md` | [#123](https://github.com/eyeskull2220/solana-invoice/pull/123) | Replaces #113. Four lanes. Leftover repo ≠ shop. Success = money in + forwardable page |

Neither is the machine. #113 is a punch-list that stop-lists its own failure modes and then schedules them. #123 names the right host and the right success test, then puts **Sell** back on **one mailbox → stop until send**.

**Success for this income seat:** money in operator accounts **and** a shop a *secretaris* forwards. Both. Zero of one is not success.

**One machine:** four lanes — Sell / Paper / Rails / Capabilities — each running **one whole job**, not 28 cells and not 11 SKUs.

**Hard locks (unchanged, not bargained):** operator is not the freelancer; no fake KBO; OFFERTE only; Phantom receive-only; Kraken paper until invert (return > 0 after fees **and** ≥ 8 fills **and** maxDD ≤ 8% on the **same** paper book, then human yes); EUR-first public face; no new connector without a written yes; no seventh seat.

---

## 1. Facts checked this run (not inventory)

| Thing | Is | Is not |
| --- | --- | --- |
| **This repo** `eyeskull2220/solana-invoice` `main` | Leftover invoice HTML: `index.html`, `catalog.html`, `solana-invoice.html`, `config.js`, README. Pay face **9 USDC**. Treasury `96BT6r5C35cvokdVop8ro4vDtv9zKUxpzrLbXqQbuHk3` | The live shop. Belgian public face. Shop progress |
| **Live shop** | [sovereignforge.surge.sh](https://sovereignforge.surge.sh/) | A euro rewrite of leftover `index.html` ([#111](https://github.com/eyeskull2220/solana-invoice/pull/111)) |
| **Live catalog (leftover host)** | [treasury-tools.surge.sh](https://treasury-tools.surge.sh/) — not the shop URL this plan uses | Proof the shop moved |

**Live shop, fetched 2026-08-27:**

- Lede: *“Charge blijft USDC op Solana. Euro is omrekening, geen checkout.”*
- Home sells four kits in USDC (900 / 199 / 199 / 349). `/pakketten.html` lists **eleven**. `/betalen.html` prints the Solana address and *“Mail het bewijs naar …”*.
- `robots.txt`: `User-agent: *` / `Disallow: /`.
- `/privacy.html`: natural person in Geel, **geen KBO**, geen vennootschap. OFFERTE, geen FACTUUR-module.
- Title already names the buyer: *“Voor de secretaris die vanavond nog een voorstel naar het bestuur stuurt.”*

**Complete EUR-first pass** (the only event that retires “until”): that **same origin** speaks euro on the public face, does not print USDC / Solana / Phantom on home / pakketten / the page a secretary forwards, is crawlable (`robots.txt` must not `Disallow: /`), stays OFFERTE. Today it fails that test. Rewriting this leftover repo does not pass it.

---

## 2. ADVERSARIAL (before any colour)

### 2.1 The punch-list is the product

#113 writes 310 lines so the last 48 hours of kits and sweeps would have a gate. Then it **is** the flood:

- **8 STARTABLE** items in parallel (CEO page, EUR shop *spec*, paper journal, OFFERTE templates, Scout RFP refresh, Wallet audit, Compliance open list, named-club demo).
- **13 BLOCKED** and **8 WAIT** rows — a second punch-list.
- **7 days × 4 lanes = 28 cells.** D1 still runs Scout + Coder + Wallet + Design + Reviewer. D3 still ships a seizoenskaart-class demo. D4 *then* “locks one” offer.
- **Concurrent Ultra wave, “do not wait, do not duplicate”:** Compliance, Wallet, Coder, Scout, Builder, web buyers, web outreach, reviewer rubric, seizoenskaart, named-club demo, hide-the-coin, design EUR-first, planner 7-day board. Thirteen seats told not to wait while the page forbids unpaged fan-out.
- Extra seats: Reviewer, Planner, Web, Design. That is a seventh-and-eighth-seat factory on a six-seat machine.
- Done metric in the last cell: *“Capabilities shipped = pages + one demo, not ten kits merged.”* Pages plus a demo is a punch-list completing itself.

A calendar of teammate chores is not a machine. A machine has four lanes and one success test. If the CEO page needs a Planner to write a second 7-day board “aligned to this page,” there is no source of truth — there are two punch-lists.

#123 correctly kills the 28 cells and the extra seats. It still dispatches “one of six” when several gaps are true. That is better than 13 Ultra seats. It is not yet one job per lane.

### 2.2 One-kit unlocks — stop-listed, then scheduled

#113 miss #3 (verbatim): club-site 900, inbox-ops 299, pipeline 399, Peppol ready 249, Peppol chase 399, review retainer 199 — *“each as its own SKU/PR — fragments the week into merge lottery. One offer on the board, or none.”*

Then the same page:

- Cites `docs/ideas-builder-ultra.md` (#81) **five** week-scale offers as dispatchable inventory (club / inbox / Peppol / pipeline / retainer).
- STARTABLE: named-club **demo**, OFFERTE templates, EUR shop spec — three artefacts beside “one offer.”
- D1 Sell: club site **or** inbox ops (already a two-SKU fork).
- D3 Sell: seizoenskaart-class demo (a third SKU family).
- D4 Capabilities: lock **one** of club **or** Peppol go-live **or** inbox.

That is one-kit logic with a Monday delay. The live shop is already the exhibit: **eleven** independent “Open voorbeeld / Betaal N USDC” rows. Unlocking one of them — or “locking one” on D4 — is the catalog, not the machine.

A secretary does not forward eleven kits. The live H1 is one voorstel naar het bestuur. Eleven SKUs are a kit factory wearing Dutch.

### 2.3 One-mail-then-hold — the actual Sell plan on both pages

#113 Sell lane:

| Day | Cell |
| --- | --- |
| D0 | **Freeze send** |
| D1 | OFFERTE skeleton. **No mail** |
| D2 | Mailto **table** only |
| D3 | Demo. **Not sent** |
| D4 | Human picks **at most one** mailbox. Agent does **not** send |
| D5 | If yes: **one send of one EUR OFFERTE**. Else WAIT |
| D6 | **0 or 1 OFFERTE in flight. No blast** |

That pattern **is** one-mail-then-hold: halt the blast, authorize a single letter, then hold. It substitutes a gated one-shot for a sell machine.

#123 replaces the grid and then writes the same act:

> “Get a forwardable EUR OFFERTE in front of one mailbox the operator picked, then **stop** until a human says send.”

The live shop already named a different commercial act: the *secretaris* forwards the page to the bestuur. Sell is that page, on that host, in euro, tonight. Operator Gmail is not the product. “One mailbox + one send” puts Eyeskull2220 back on as the freelancer — the lock both pages claim to keep.

A sibling seat already sent from the bound mailbox to club `info@` (same-hour burst 2026-08-27, cited in #113 miss #6). Freezing that blast is correct. Replacing it with **one** operator letter is not a machine. It is the blast with `limit 1`.

### 2.4 Leftover repo scored as shop movement

#113 inventories Ultra PRs in **this** repo as what CEO can dispatch, and treats EUR shop as a Design/Builder/Reviewer spec **in this leftover tree**. `main` is still `Solana Invoice — 9 USDC`.

#111 rewrites leftover `index.html` / `catalog.html` into euro OFFERTE pages. That is a false shop. The live origin did not move.

#112’s rubric is useful as a miss list (USDC on face, `Disallow: /`). Scoring leftover `index.html` as if it were SovereignForge is the same category error: this repo is not the shop.

#123 states the split correctly. Any plan that still counts #111 / #113 / kit PRs / `treasury-tools.surge.sh` as Sell progress is RED on location.

### 2.5 Theatre around the invert gate and the loket

Paper numbers are not money in. A VOORBEELD journal PR is not the shop. Kraken live while the connector is dead is a miss. Those locks are right.

They do not rescue a punch-list. Filling invert-gate counters and a Compliance open-work list in the same week as “one kit unlock” and “one mail” is how a board looks busy while the secretary still cannot forward a euro page and no settlement has landed.

---

## 3. RGY

Colour rules for **this** file:

- **RED** — the plan, as written, *is* one-kit unlocks, *or* one-mail-then-hold, *or* treats leftover Ultra PRs as shop progress, *or* is a punch-list board instead of one machine.
- **YELLOW** — the plan names the miss and still schedules a smaller version of it (delay, “or none”, “one of”).
- **GREEN** — the plan is four lanes, whole jobs, NOW/NEXT/WAIT, live shop as Sell, leftover repo idle, no kit SKU as the week, no operator letter as the week.

| Claim | Exhibit | Score |
| --- | --- | --- |
| **Punch-list boards vs one machine** | #113: 28 cells + 8 STARTABLE + 13 Ultra “do not wait.” #123: four lanes, still “one of six” when several gaps are true | **RED** on #113. **YELLOW** on #123 (machine named, chores not fully killed) |
| **One-kit unlocks** | Live shop 11 SKUs. #113 D4 lock-one of three families + five ideas-builder offers. #123 stop-lists SKUs then still allows “another SKU” as the miss rather than deleting the catalog-as-plan | **RED** |
| **One-mail-then-hold** | #113 D0–D6 freeze → one mailbox → one send → 0-or-1. #123 Sell = one mailbox then stop until send | **RED** |
| **Leftover repo as shop** | This `main` 9 USDC. #111 euro face on leftover HTML. #113 dispatch inventory of Ultra PRs here. Live shop is sovereignforge.surge.sh and still USDC + `Disallow: /` | **RED** |
| **Success = money in + forwardable page** | Stated in #123. Not the done-metric in #113 (*pages + one demo*). Neither page has money in. Live page is not yet forwardable (USDC on face, blocked crawlers) | **YELLOW** as a sentence in #123; **RED** as a result |
| **EUR-first live pass** | Specified; not live. Specs and leftover rewrites do not count | **RED** until the origin changes |
| **Operator not freelancer** | Locked on both pages; contradicted by operator-as-sender Sell | **RED** while Sell is mail |
| **No fake KBO / OFFERTE only** | Held on live privacy + both plans | **GREEN** as a lock (not as income) |
| **Phantom receive-only / no new connector** | Held | **GREEN** as a lock |
| **Kraken paper until invert gate** | Held; connector unhealthy this wave. Paper ≠ shop | **GREEN** as a lock; **RED** if counted as shop progress |
| **Six seats, no seventh** | #113 adds Reviewer/Planner/Web/Design. #123 restores six | **RED** on #113. **GREEN** on #123’s seat table |
| **Grokbot Linear as this income seat** | Both plans exclude it. Keep excluded | **GREEN** as a lock |

**Overall on the existing CEO plans: RED.**  
They are punch-list boards (or a board that still sells by one mail), they unlock kits, they treat this leftover repo as movement, and they do not yet produce money in or a secretary-forwardable euro shop.

GREEN is not available until the design-out below is the plan — and the live origin matches Sell NOW.

---

## 4. Design-out — NOW / NEXT / WAIT per lane, whole jobs

Four lanes. One whole job in NOW. One whole job in NEXT. WAIT is a halt, not a backlog of chores. No daily cells. No fourth lane named “sweeps.” No seventh seat.

A **whole job** ends in something the operator can point at: a live origin, a readable paper book, a silent rail, a team that can run those three without a 28-cell calendar. A whole job is not a spec, not a demo file, not a kit SKU, not one operator letter.

### Sell — the shop a secretaris forwards

The commercial act is already on the live H1: the secretary forwards a voorstel to the bestuur. **The page is the send.** Operator Gmail is not the sell machine.

| | Whole job |
| --- | --- |
| **NOW** | Complete EUR-first pass on **[sovereignforge.surge.sh](https://sovereignforge.surge.sh/)**. One origin. Euro on home / pakketten / the forwardable OFFERTE. No USDC / Solana / Phantom on those surfaces. `robots.txt` must not `Disallow: /`. Stamp OFFERTE. Privacy can stay “geen KBO.” Settlement copy stays on betalen **after** agreement, not on the page the secretary forwards. Builder does this on **that host**, not in this leftover repo. |
| **NEXT** | Collapse the public face to **one job** that H1 already named (club/vzw voorstel-voor-bestuur). Other SKU rows are not the week. A secretary forwards one page, not eleven unlocks. |
| **WAIT** | Operator cold-mail; one-mailbox-then-send; Gmail blast; leftover-repo euro rewrite (#111 class); FACTUUR; USDC in the forwardable face; treating kit hosts as the shop |

**Not the plan:** freeze → pick one mailbox → one send → hold. That scores RED in §3 even when the letter is EUR and OFFERTE.

### Paper — one book that can pass or fail invert

| | Whole job |
| --- | --- |
| **NOW** | One paper book (`dca-paper`, do not reset) with invert counters readable on the same book: fills, return after fees, maxDD. Coder stamps. Whole job = the book can be read. |
| **NEXT** | Sessions into **that** book until the gate can fire or fail. New idea → new book (`fib-paper`, `grid-paper`). Never mix leverage into `dca-paper`. |
| **WAIT** | Live Kraken; invert without all three numbers **and** healthy connector **and** human yes; counting a journal PR as shop progress or money in |

Paper is not Sell. Paper numbers are not money in.

### Rails — one receive path, silent on the shop face

| | Whole job |
| --- | --- |
| **NOW** | Receive-only freeze of the **one** known treasury string. Do not print it on the forwardable shop face. Wallet owns evidence of money in. Phantom: no send, swap, rebalance, perps, new key, third address. |
| **NEXT** | Operator loket conversation (Track A / bijberoep default while employee hours hold) when the **human** chooses. This file does not file, e604, or invent a number. |
| **WAIT** | Fake KBO / BTW / Peppol `0208:`; FACTUUR; Phantom send “to test”; new connector; treasury address as shop copy; booking USDC as “not income” after a real loket |

### Capabilities — six seats that can run the other three lanes

| | Whole job |
| --- | --- |
| **NOW** | Six seats only (CEO, Builder, Scout, Coder, Wallet, Compliance). This file (and #123’s leftover-vs-shop split) is the stop. Kill punch-list boards, Ultra waves, and Reviewer/Planner/Web/Design as seats. Whole job = the team can be named on one page without a 28-cell calendar. |
| **NEXT** | CEO dispatches **Builder** for Sell NOW on the live origin. One teammate, pointer to this file. Scout may produce **one** public-mailbox **table** (research). Scout does not send. |
| **WAIT** | Kit SKU merge “so something ships”; second Ultra wave; Grokbot Linear tickets for this income; leftover `solana-invoice` HTML as capability proof; a seventh seat |

**Dispatch rule:** if Sell NOW is open, that is the only Capabilities NEXT. Two parallel unlocks is the kit factory. CEO may stop. CEO may not mail. CEO may not edit kit HTML from this seat.

---

## 5. What is not this plan

**Not shop progress**

- Any Ultra / kit / euro-face PR under `eyeskull2220/solana-invoice` (#81–#83, #111, #113, kit SKUs, sweeps, paper journal).
- `treasury-tools.surge.sh` and `solana-invoice-treasury.surge.sh`.
- A FAIL rubric that scores leftover `index.html` as SovereignForge (#112 class) — keep as a miss list, not a shop scoreboard.

**Not a board**

- 7 × 4 cells.
- STARTABLE / BLOCKED / WAIT chore tables.
- “Capabilities shipped = pages + one demo.”

**Not an unlock**

- Merging one kit because it looks complete.
- FACTUUR, KBO, Peppol send, or Kraken live because a file *looks* ready.
- One operator letter as the week’s commercial act.

---

## 6. This run

| Did | Did not |
| --- | --- |
| Adversarial pass on #113 and #123 | Mail |
| RGY: RED on punch-list / one-kit / one-mail-then-hold / leftover-as-shop | Edit `index.html` / `catalog.html` / kit HTML |
| Design-out NOW/NEXT/WAIT per lane as whole jobs | Count #111 / #113 / kit PRs as shop movement |
| Named live shop `sovereignforge.surge.sh` vs leftover `main` | Add a seventh seat; send; invent KBO |

Next: Builder on [sovereignforge.surge.sh](https://sovereignforge.surge.sh/) for Sell NOW, with a pointer here — or nothing.

---

## 7. Sources (this file)

- Repo `main` @ `2170952`: README, `catalog.html`, `config.js`, `index.html` (USDC-first leftover).
- Live origin fetches 2026-08-27: `/`, `/pakketten.html`, `/betalen.html`, `/privacy.html`, `/robots.txt`.
- [#113](https://github.com/eyeskull2220/solana-invoice/pull/113) `docs/ultra-seats/CEO.md` (28-cell board, STARTABLE list, D4 lock-one, D5 one send).
- [#123](https://github.com/eyeskull2220/solana-invoice/pull/123) `docs/rgy-2026-08-27/ceo/FIX-BOARD.md` (leftover vs shop; Sell still one-mailbox-then-hold).
- [#111](https://github.com/eyeskull2220/solana-invoice/pull/111) leftover euro-face rewrite (false shop).
- [#112](https://github.com/eyeskull2220/solana-invoice/pull/112) reviewer FAIL (USDC on live face; `Disallow: /`).
- [#81](https://github.com/eyeskull2220/solana-invoice/pull/81) five Builder offers (inventory, not this week’s unlocks).

**PII:** No personal mailbox, phone, or home street is copied here. The treasury string stays a Rails fact, not shop copy.

End of CEO PLAN. Whole jobs, four lanes, or nothing.
