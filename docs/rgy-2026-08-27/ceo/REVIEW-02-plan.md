# REVIEW-02-plan — CEO PLAN pack

**Seat:** Reviewer  
**Batch:** new (CEO PLAN only)  
**Date:** 2026-08-27  
**Artifact:** PR [#160](https://github.com/eyeskull2220/solana-invoice/pull/160) `docs/rgy-2026-08-27/ceo/02-adv-plan.md` (head `0773af2`)  
**This file is judgment only.** No implement. No mail. No kit HTML. No design review. No code review.

**Overall: YELLOW.** GREEN is refused while any yellow remains on this pack.

Existing CEO pages stay **RED** (confirmed below). That is the exhibit the pack scores. It is **not** this pack’s grade. This pack designs the three named misses out of Sell, then still schedules a second Capabilities NEXT job. That leftover is yellow.

---

## What was scored (and what was not)

| Object | Used as |
| --- | --- |
| PR #160 `02-adv-plan.md` | **The plan pack.** Only object this file grades. |
| PR [#113](https://github.com/eyeskull2220/solana-invoice/pull/113) `docs/ultra-seats/CEO.md` | Exhibit the pack scores RED (punch-list / one-kit / one-mail-then-hold). Read 2026-08-27. |
| PR [#123](https://github.com/eyeskull2220/solana-invoice/pull/123) `docs/rgy-2026-08-27/ceo/FIX-BOARD.md` | Exhibit the pack scores RED on mail, YELLOW on leftover chores. Read 2026-08-27. |
| Live [sovereignforge.surge.sh](https://sovereignforge.surge.sh/) (`/`, `/pakketten.html`, `/betalen.html`, `/privacy.html`, `/robots.txt`) | Fact-check of the pack’s location claims. **Not** a shop/design/code review. |
| This repo `main` (README, leftover HTML, 9 USDC) | Fact-check: leftover ≠ shop. **Not** scored as product. |
| Shop trees, kit HTML, Builder/Scout/Wallet/Compliance packs | **Out of scope.** |

Colour rules applied **to this pack** (same rules the pack writes for plans):

- **RED** — the pack *is* one-kit unlocks, *or* one-mail-then-hold, *or* leftover Ultra PRs as shop progress, *or* a punch-list board instead of one machine.
- **YELLOW** — the pack names the miss and still schedules a smaller version of it (delay, “or none”, “one of”, a second NEXT job).
- **GREEN** — four lanes, whole jobs, NOW/NEXT/WAIT, live shop as Sell, leftover repo idle, no kit SKU as the week, no operator letter as the week. **GREEN only if this table has no red and no yellow.**

---

## Verdict table (this pack)

| # | Check | Grade | Notes |
| --- | --- | --- | --- |
| 0 | **Overall plan pack** | **YELLOW** | Sell is a whole job on the live origin. Existing pages’ RED scores hold. Capabilities NEXT is two jobs. |
| 1 | Punch-list boards vs one machine **in this pack** | **YELLOW** | NOW/NEXT/WAIT per lane replaces the 28-cell grid. Capabilities NEXT still lists Builder **and** a Scout mailbox table. That is a chore beside a whole job. |
| 2 | One-kit unlocks **in this pack** | **GREEN** | No D4 lock-one. No kit SKU as the week. Sell NEXT collapses the face to the live H1, not “one of three families.” Kit merge is WAIT. |
| 3 | One-mail-then-hold **in this pack** | **GREEN** | Sell NOW = the page a *secretaris* forwards. Operator Gmail is WAIT. Freeze → one mailbox → one send is named RED even if EUR/OFFERTE. Scout “does not send.” |
| 4 | Leftover repo as shop **in this pack** | **GREEN** | `main` idle. Builder on [sovereignforge.surge.sh](https://sovereignforge.surge.sh/). #111 / #113 / kit PRs listed as not shop progress. |
| 5 | Four lanes, whole jobs, NOW/NEXT/WAIT | **YELLOW** | Shape is right. “One whole job in NEXT” is broken on Capabilities (two items in one NEXT cell). |
| 6 | This review mailed / edited kits / reviewed design or code | **GREEN** | None of those. |

**RED rows on this pack: none.**  
**YELLOW rows on this pack: 0, 1, 5.**  
GREEN is therefore refused.

---

## Existing CEO pages — exhibit check (not this pack’s grade)

The pack’s overall on **#113 and #123** is **RED**. That score is confirmed. Do not treat those pages as the machine.

### Punch-list boards — RED on the exhibits

|#113 `CEO.md` (read)| Pack’s claim | Check |
| --- | --- | --- |
| STARTABLE table | 8 STARTABLE | **8 rows** (CEO page, EUR spec, paper journal, OFFERTE templates, Scout RFP, Wallet audit, Compliance open list, named-club demo). |
| Concurrent Ultra wave | 13 seats told not to wait | **13 named:** Compliance, Wallet, Coder, Scout, Builder, web buyers, web outreach, reviewer rubric, seizoenskaart, named-club demo, hide-the-coin, design EUR-first, planner 7-day board. |
| 7 × 4 board | 28 cells | D0–D6 × Sell/Paper/Rails/Capabilities. D1 still runs Scout + Design + Reviewer. D3 ships a seizoenskaart-class demo. D4 then “locks one.” |
| Extra seats | seventh-seat factory | Reviewer, Planner, Web, Design sit in the inventory table. |
| Done metric | pages + one demo | Last cell: *“Capabilities shipped = pages + one demo, not ten kits merged.”* |

|#123 `FIX-BOARD.md` (read)| Pack’s claim | Check |
| --- | --- | --- |
| Four lanes named | machine named | Sell / Paper / Rails / Capabilities. Extra seats killed. Leftover vs shop split is correct. |
| Still chores | “one of six” when several gaps are true | Dispatch rule: live shop USDC → Builder; no page → Builder; no mailbox → Scout; no money in → Rails wait; paper unread → Coder. **If two are true, do one.** That is a chore picker, not one whole job per lane. |

Pack’s colours on this row (**RED** on #113, **YELLOW** on #123) hold. Overall on existing pages **RED** holds.

### One-kit unlocks — RED on the exhibits

#113 miss #3 stop-lists club-site / inbox / pipeline / Peppol / retainer as merge lottery, then the same page:

- cites #81 five week-scale offers as dispatchable inventory
- STARTABLE: named-club **demo** + OFFERTE templates + EUR **spec** beside “one offer”
- D1 Sell: club site **or** inbox ops
- D3 Sell: seizoenskaart-class demo
- D4: lock **one** of club **or** Peppol go-live **or** inbox

Live shop, fetched this run (not a design review): `/pakketten.html` still headlines **Elf live kits** with independent “Open voorbeeld / Betaal N USDC” rows. Home still sells four USDC kits (900 / 199 / 199 / 349). Unlocking one row, or locking one family on D4, is the catalog.

#123 stop-lists SKUs and still allows “another SKU” as the miss to avoid, rather than deleting catalog-as-plan. Pack’s **RED** on this row holds.

### One-mail-then-hold — RED on the exhibits

#113 Sell cells, verbatim pattern:

| Day | Cell |
| --- | --- |
| D0 | Freeze send |
| D1 | OFFERTE skeleton. No mail |
| D2 | Mailto **table** only |
| D3 | Demo. Not sent |
| D4 | Human picks **at most one** mailbox. Agent does **not** send |
| D5 | If yes: **one send of one EUR OFFERTE**. Else WAIT |
| D6 | **0 or 1 OFFERTE in flight. No blast** |

#123 Sell, verbatim: *“Get a forwardable EUR OFFERTE in front of one mailbox the operator picked, then **stop** until a human says send.”*

That is the blast with `limit 1`. Pack’s **RED** holds. Operator-as-sender also contradicts the “operator is not the freelancer” lock both pages claim.

### Leftover repo as shop — RED on the exhibits

This `main` (this run): leftover invoice HTML, pay face **9 USDC**, treasury in README. Not the shop.

Live origin (this run, facts only):

- Lede still *“Charge blijft USDC op Solana. Euro is omrekening, geen checkout.”*
- `robots.txt`: `User-agent: *` / `Disallow: /`
- `/betalen.html` prints the Solana address and *“Mail het bewijs naar …”*
- `/privacy.html`: natuurlijke persoon in Geel, geen KBO, geen vennootschap, OFFERTE, geen FACTUUR-module
- Title already names the buyer: *“Voor de secretaris die vanavond nog een voorstel naar het bestuur stuurt.”*

#111 euro rewrite of leftover `index.html` is a false shop. Pack’s location split holds.

---

## YELLOW on this pack (why GREEN is refused)

The pack’s own YELLOW rule: *names the miss and still schedules a smaller version of it.*

### Y1 — Capabilities NEXT is two jobs

Pack §4: *“Four lanes. One whole job in NOW. One whole job in NEXT. WAIT is a halt, not a backlog of chores.”*

Capabilities NEXT (one cell) writes both:

1. CEO dispatches **Builder** for Sell NOW on the live origin. One teammate, pointer to this file.
2. **Scout may produce one public-mailbox table** (research). Scout does not send.

That second sentence is #113 D2 (mailto table) and #123 Scout (“one public mailbox category”) with send stripped. The pack scores #123 **YELLOW** for *“machine named, chores not fully killed.”* Then it keeps the leftover chore in its own NEXT cell.

The dispatch rule tries to save it: *“if Sell NOW is open, that is the only Capabilities NEXT. Two parallel unlocks is the kit factory.”* Sell NOW is open today (origin still USDC + `Disallow: /`). A rule that contradicts the cell is not one whole job in NEXT. It is a smaller #123.

**Exact design-out for GREEN on this row:** Capabilities NEXT = **only** Builder on the live origin (or nothing). Move the Scout table to **WAIT** with operator cold-mail / one-mailbox-then-send. Do not list it in NEXT.

No other yellow on the pack. Do not invent a second one to keep a punch-list alive.

---

## RED on this pack

**None.**

This file is not a 28-cell board. It does not unlock a kit SKU as the week. It does not make operator Gmail the Sell job. It does not count leftover Ultra PRs as shop movement.

Do not read “existing pages RED” as “this pack RED.”

---

## GREEN on this pack (what already holds)

These rows are green **as writing**. They do not make the overall GREEN while Y1 remains.

### G1 — Sell is the page, not the mailbox

Sell NOW: complete EUR-first pass on **the live origin**. Euro on home / pakketten / the forwardable OFFERTE. No USDC / Solana / Phantom on those surfaces. `robots.txt` must not `Disallow: /`. Stamp OFFERTE. Settlement copy on betalen **after** agreement. Builder on **that host**.

Explicit kill: freeze → pick one mailbox → one send → hold scores **RED** even when the letter is EUR and OFFERTE. Operator Gmail is WAIT. That is the miss #113/#123 still are.

### G2 — One-kit is WAIT, not delayed Monday

No D4 “lock one of three.” Sell NEXT is collapse to the job the live H1 already named (club/vzw voorstel-voor-bestuur), not merge-one-SKU. Capabilities WAIT: kit SKU merge “so something ships.” That is not one-kit-with-a-delay.

### G3 — Leftover repo idle

§5 names #81–#83, #111, #113, kit SKUs, sweeps, paper journal, `treasury-tools.surge.sh`, `solana-invoice-treasury.surge.sh` as **not shop progress**. #112 stays a miss list, not a shop scoreboard. This review agrees.

### G4 — Locks that stay locks (not income)

No fake KBO / OFFERTE only. Phantom receive-only. Kraken paper until invert on the **same** book (return > 0 after fees **and** ≥ 8 fills **and** maxDD ≤ 8% **and** human yes). Six seats, no seventh. Grokbot Linear excluded. Paper numbers are not money in. This pack does not file, e604, or invent a number.

### G5 — This run’s own scope

Pack did not mail. Pack did not edit kit HTML. This review matches that.

---

## GREEN looks like (plan pack only)

Do **all** of these on `02-adv-plan.md`. Partial NOW/NEXT/WAIT with a second NEXT item is still YELLOW.

1. **Capabilities NEXT is one whole job.** Builder on [sovereignforge.surge.sh](https://sovereignforge.surge.sh/) for Sell NOW, pointer here — or nothing. No Scout table in that cell.
2. **Scout mailbox table sits in WAIT** with operator cold-mail, one-mailbox-then-send, Gmail blast. Research that is not the week is WAIT, not NEXT.
3. **Keep what already holds:** four lanes; NOW/NEXT/WAIT; live shop as Sell; leftover `solana-invoice` idle; no kit SKU as the week; no operator letter as the week; six seats; no seventh.

Until 1–2 are in the pack, this review stays **YELLOW**. Reviewer does not rewrite PR #160. Reviewer does not mail. Reviewer does not port Surge.

Live origin matching Sell NOW is **Builder**, not a plan-pack GREEN. Do not hold this file until the shop moves. Do not GREEN this file because the shop moved.

---

## Notes

- **Success = money in + forwardable page** is still the done test. Neither exhibit has money in. Live page is not yet forwardable (USDC on face, crawlers blocked). That is a **result** RED, not a defect in this pack’s Sell NOW sentence.
- **#123’s leftover-vs-shop split** is a correct fact. Capabilities NOW may cite it. Capabilities NOW must not re-seat #123 as “the machine” — Sell there is still one-mail-then-hold. Pointer for teammates is **this pack**, after Y1 is killed, or nothing.
- **Treasury string** in the pack’s facts table is a Rails fact. It is not shop copy. This review does not reprint pay-page mailboxes.
- **PII:** no personal mailbox, phone, or home street copied here.

---

## This run

| Did | Did not |
| --- | --- |
| Read PR #160 `02-adv-plan.md` as the only graded object | Mail |
| Read #113 `CEO.md` and #123 `FIX-BOARD.md` as exhibits | Edit kit HTML / leftover `index.html` / shop trees |
| Fact-checked live origin + this `main` for location claims | Review design or code |
| Confirmed RED on punch-list / one-kit / one-mail-then-hold **on existing pages** | Treat #111 / #113 / kit PRs as shop movement |
| Scored **this pack YELLOW** (Capabilities NEXT dual-job) | Rewrite #160; add a seventh seat; send; invent KBO |

**Plan pack (PR #160): YELLOW. Not GREEN.**
